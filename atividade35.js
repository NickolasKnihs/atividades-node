// 35. Calcular o número de Euler (e) elevado a um número.
function main (numero){
    // Calcular e elevado ao número fornecido
    const resultado = Math.exp(numero);

    return resultado;
}

// console.log(main(2));
module.exports = main;